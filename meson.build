project('wa_game', 'c',
    default_options: [
        'werror=true',
        'warning_level=2',
        'default_library=static',
        'c_std=c17'
    ]
)

subdir('lib/')
subdir('client/')
subdir('server/')
